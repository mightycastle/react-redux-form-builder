import FormBuilderSteps from './FormBuilderSteps';

export default FormBuilderSteps;

/*
wrapper component for determining if we are on
'select', 'arrange', 'configure' or 'send' step
and displaying the correct one
*/
